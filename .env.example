# Get a key from https://console.cloud.google.com/apis/credentials
# after enabling the "YouTube Data API v3" for your project.
# Restrict it (HTTP referrer or IP) before using it anywhere but localhost.
YOUTUBE_API_KEY=

# Optional, defaults to 3000
PORT=3000
